@page
@model LMS.Pages.Studen.IndexModel
@{
    ViewData["Title"] = "Index";
}




<form method="post" >
    <div style="padding:100px;" >


        @if (Model.Message != null)
        {

            <div class="alert alert-danger alert-dismissable" style="border-radius:10px;">
                <p>@Model.Message</p>


            </div>

        }

        <div class="form-group ">
            <label style="font-size:30px;">loggin page</label>
            <input id="one" required type="number" min="0"  asp-for="Studentloggin.Numone" class="form-control" style="width:200px;" placeholder="گذروازه">
        </div>
        <div class="form-group">
            <input id="two" required type="number" min="0"  asp-for="Studentloggin.Numtwo" class="form-control" style="width:200px;" placeholder="رمز">
        </div>


        <button type="submit" class="btn btn-success">
            ورود
        </button>
        <br />
        <br />

        <span>گذروازه و رمز برای دانشجویان شماره دانشجویی است</span>

        <!-- onclick="
        var pw = document.getElementById('one').value;
        var pw = document.getElementById('two').value;
        if (one != two) {
            alert('گذروازه و رمز متفاوت هستند...');

        }
    -->

    </div>

   
</form>